#include <bits/stdc++.h>

using namespace std;

float x;

int main()
{
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

    cin >> x;

    cout << (int) x;

    return 0;
}